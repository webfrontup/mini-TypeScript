console.log(documentTitle)
setTitle('yyccQQu')
console.log(getTitle())
// 处理库声明文件的第一部分
// 对修改全局的模块写声明文件
import '../ts-modules/add_methods_to_string.js'
const name = 'yyccQQu';
console.log(name.getFirstLetter())
//    // 依赖引入
//    /// <reference types="moment"/>

// 快捷模块声明
// 在typings文件夹下写入对应声明文件代码
